# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
backup
*.backup

# Exclude crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version control
# as they are data-sensitive and environment-specific.
*.tfvars
*.tfvars.json

# Ignore override configuration files as they are usually used to override resources locally.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
